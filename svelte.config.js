/** @type {import('@sveltejs/kit').Config} */
// Kit options (adapter, paths, csp, prerender) live in vite.config.ts —
// when they are passed via the Vite plugin, this file is ignored for kit.*.
const config = {};

export default config;
